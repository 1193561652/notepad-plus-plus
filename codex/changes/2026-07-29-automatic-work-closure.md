# 非人工工作收口

## 代码

- `UiParityCapture` 使用真实 Mark 和 Preferences 动作，不再直接构造对话框。
- 支持隔离配置目录和确定性的 `zh_CN` 语言模式。
- 补齐 Preferences 自动插入、备份目录、文件关联和标签匹配控件标识。
- 补齐对应简体中文资源及文件关联分类名称。

## 测试

- `find-dialog-localization-tests` 增加 Preferences 控件标识和资源检查。
- 新增简体中文浅色/深色、100%/150% 四组 CTest UI 运行矩阵。
- 全量构建成功，CTest `30/30` 通过。

## 范围

插件系统和大文件方案 B 按用户要求排除。其余尚未闭环的项目均需要目标系统、
网络端点、物理设备、大内存压力环境或主观视觉判断。
