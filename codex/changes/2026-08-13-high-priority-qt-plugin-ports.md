# 2026-08-13 高优先级插件 Qt 首版移植

## 范围

- 同步八个插件官方 tag 到用户 fork，并在选定基线 tag 上创建 `qt-port`。
- 保留原版源码结构，在独立 `qt/` 目录实现跨平台版本。
- 所有插件名追加 `-qt`，并在 Windows 上保留空实现的原 Notepad++ ABI 导出。

## 验证

- 八个插件均完成 Debug 构建和独立核心/动态 ABI CTest，共 13/13 通过。
- ABI 测试验证新命令表可调用，并验证旧 `getFuncsArray()` 返回空表。
- 八个产物同时部署到 Qt 主程序隔离配置，正常 Windows 平台启动后保持运行并正常退出。
- Qt 主程序全量 CTest 为 47/47 通过。

## 结论

本批次建立了八个插件的可加载、可操作首版和持续测试基线。主要用户流程已实现，
但复杂插件仍有深层功能差异，详见 `codex/index/high-priority-plugin-ports.md`，后续不能
将当前状态视为与原版完全等价。
